package string;

public class MiddleCharToUpperCase {
/*
 * /2)Take a sentence as input and for the word with length odd convert 
 * the middle character to uppercase 
 * and for the word length even convert the
 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		 String str = "Hello World";
		 int count = 0;
		 int middle = count;
		 for (int i = 0; i< str.length(); i++) {
			
					 count ++;
				
					
		 } 
		 //int mid =count/2;
		 //find middle element
		 System.out.println(count);
		 
		 
		// System.out.println("Middle string upper case is " + str.toUpperCase().substring(mid,1) + " from " + str);
	}

}
